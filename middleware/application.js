export default function(ctx, i18n) {
  // console.log('application middleware', i18n.locale)
    // if(ctx.route.path === '/' && process.env.LANG !== 'ca') {
    //   ctx.redirect(301, `/${process.env.LANG}`)
    // }
}